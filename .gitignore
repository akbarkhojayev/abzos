# Build outputs (multi-GB, regenerated by scripts/build_iso.sh)
*.iso
*.iso.sha256
/image/

# Legacy leftovers pending manual `sudo rm -rf` (see README "Known local cleanup pending")
/chroot/
/debian_chroot/

# QEMU boot-test artifacts (regenerated by scripts/verify_iso_boot.py)
/iso_verify.png
/verify_output.txt

# Harness state
/.claude/
